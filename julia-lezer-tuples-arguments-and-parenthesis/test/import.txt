# Tuples

(
  ();
  (a);
  (a,);
  (a,b,c,);
  (a,b,c);
)

==>

SourceFile(InlineBlock(Tuple,InlineBlock(Identifier),Tuple(Identifier),Tuple(Identifier,Identifier,Identifier),Tuple(Identifier,Identifier,Identifier)))
